# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase project settings are public, but local environment files may
# contain other credentials. Never commit them; commit .env.example only.
.env
.env.*
!.env.example

# Download this from the existing voiceofprophecy Firebase Android app
# registered as com.sda.vop. Supply it in release CI through an approved secret.
android/app/google-services.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android build outputs and caches
android/.gradle
android/build
android/app/build
android/local.properties
.gradle

# Firebase Functions compiled output
functions/lib
