name: VOP Web Build Validation

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - fix/real-admin-data-and-management-ui
      - feature/admin-managed-content

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm install --ignore-scripts
      - run: npm run build
